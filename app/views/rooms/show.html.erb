<div class="container room-show">
    <div class="row">
        <div class="col-md-8">
<% if @room.image.attached? %>
  <%= image_tag @room.image ,size: "300x250" %>
<% end %>
            <div class="room-show-detile">
                <div class="room-show-detile-icon">
                     <img src="https://presite-potepancamp-rails-02.herokuapp.com/assets/default_icon.jpg" class="host-icon">

                </div>

                <div class="room-show-profile">
                    <p class="room-show-profile-name">
                    部屋の名前:
                        <%= @room.room_name %>
                    </p>
                    <p class="room-show-profile-address">
                    場所：
                        <%= @room.address %>
                    </p>
                </div>
            </div>
</div>
            <p class="room-show-detile-content">
            部屋の紹介：
                <%= @room.introduction %>
            </p>
        </div>

        <div class="col-md-4">
            <div class="room-show-nav">
         <%= form_with model: @reservation, method: :get, url: new_room_reservation_path(@room.id) do |f| %>
         <!--(@room.id)はactioncontroller エラーで、missing required keys:room_idと記載があったため、(room_idが必要だけどないよっと言われている)roomのidをpshtに渡す必要があるため、roomのidで＠room_id　。
         ちなみに、bookとroomのidが必要と言われれば、(@book, room.id)と記載 -->

                <%= f.hidden_field :room_id, :value => @room.id %>

                <%= f.label :room_id ,"部屋ID"%>
                <h5 class="price-icon"><%= @room.price %> / １日（1泊の金額）</h5>
                <!--　一泊の金額 -->

                    <div class="room-show-date-select">
                        <%= f.label :room_id ,"部屋ID"%>
                        <p><%= @room.id%>/ID</p> <br>




                        <%= f.label :start_date ,"開始日"%>
                        <%= f.date_field :start_date ,class:"form-control mb-4" %>

                        <%= f.label :end_date ,"終了日"%>
                        <%= f.date_field :end_date ,class:"form-control mb-4" %>

                        <%= f.label :people ,"人数"%>
                        <%= f.number_field :people ,class:"form-control mb-4" %>

                        <%= f.submit "予約する",class:"form-control btn btn-primary"%>
                    </div>
                <% end %>
            </div>
        </div>
    </div>
</div>